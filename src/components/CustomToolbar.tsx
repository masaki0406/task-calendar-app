// src/components/CustomToolbar.tsx

'use client';

import { ToolbarProps } from 'react-big-calendar';
import { Task } from '../types/task'; // Task型が別ファイルで定義されている前提（なければ定義を記載）

const CustomToolbar = (_props: ToolbarProps<Task, object>) => null;

export default CustomToolbar;
